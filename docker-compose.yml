services:
  web:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app
      - ./workspace:/app/workspace
    environment:
      - FLASK_DEBUG=1
      - MUSTACHE_WORKSPACE=/app/workspace
