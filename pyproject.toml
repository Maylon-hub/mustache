[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mustache-core"
version = "0.3.0"
description = "MustaCHE (Multiple Cluster Hierarchies Explorer) integrated with pre-compiled Core-SG"
authors = [
    {name = "Maylon Martins de Melo", email = "maylonmmartins@estudante.ufscar.br"},
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0.0",
    "core-sg-mustache>=0.4.2",
    "numpy",
    "pandas",
    "scikit-learn",
    "scipy",
    "plotly"
]

[project.scripts]
mustache = "mustache.cli:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["mustache*"]
exclude = ["tests*", "docs*", "scripts*", "legacy*"]

[tool.setuptools.package-data]
mustache = ["static/**/*", "templates/**/*"]

[tool.setuptools.exclude-package-data]
mustache = ["*.pdf", "*.bat", "*.sh"]
# Removido *.png e *.jpg para preservar as imagens da Web UI

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-flask>=1.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_clustering.py", "test_batch.py", "test_hai.py", "test_api_routes.py"]
addopts = "-v --tb=short -p no:flask"
